#import "../media.graphql"

query GetLegacyNews($id: String = "", $status: Boolean = false) {
  legacyNews(filter: { id: $id, status: $status, type: "article" }) {
    results {
      ... on NodeArticle {
        id
        title
        created {
          time
          timestamp
        }
        body {
          processed
        }
        heroImage {
          ... on MediaImage {
            path
            image {
              alt
              height
              url
              width
            }
          }
        }
      }
    }
  }
}

